---
import { profile } from '../data/portfolio';
---

<section class="section" id="about">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">About</span>
      <h2>Engineer of <span class="accent">configurable</span> systems &amp; intelligent interfaces.</h2>
    </div>

    <div class="about-grid">
      <div class="about-bio reveal" data-delay="1">
        <p>
          I specialize in <b>ReactJS, Next.js, and TypeScript</b>, with a focus on
          <b>schema-driven, configurable frontend architecture</b>. At <b>LYIK Technologies</b> —
          where I was recently promoted to <strong class="acc">Senior Engineer</strong> — I architect a
          configurable enterprise form-filling platform, its admin dashboard, and ERP workflows.
        </p>
        <p>
          On v3 I built the <b>admin dashboard</b> — form and user management with a tree-based org
          hierarchy and full REST CRUD — a permission <b>guardrail</b> system, and the
          <b>maker-checker</b> approval flow, while working across the form
          <b>engines</b> (rules, derive, actions, navigator) and their test suites. The thread through
          all of it: turning complex requirements into systems that
          <strong class="acc">feel effortless to use</strong>.
        </p>
      </div>

      <div class="about-side">
        <div class="card stat-card reveal" data-delay="2">
          <div class="glow" aria-hidden="true"></div>
          <div class="stat-num">30%+</div>
          <div class="stat-label">efficiency gains delivered through client-facing platform development</div>
        </div>
        <div class="card fact-card reveal" data-delay="3">
          <div class="fc-label"><svg class="ic" data-lucide="graduation-cap"></svg> Education</div>
          <div class="fc-value">{profile.education} <span class="muted">— {profile.educationSchool}</span></div>
        </div>
        <div class="card fact-card reveal" data-delay="3">
          <div class="fc-label"><svg class="ic" data-lucide="languages"></svg> Languages</div>
          <div class="chip-row">
            {profile.languages.map((l) => <span class="chip">{l}</span>)}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
